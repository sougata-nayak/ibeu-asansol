import 'package:flutter/material.dart';

class IndexListScreen extends StatelessWidget {
  const IndexListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
